#include <Matrix.h>

template<typename T, int M, int N>
void Matrix<T, M, N>::debugPrint() {
    for (int i = 0; i < M; ++j) {
        for (int j = 0;j < N; ++j) {

        }
    }
}
// template<typename T, int M, int N> void Matrix<T, M, N>::debugPrint();
